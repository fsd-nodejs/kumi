import { defineConfig } from 'umi'

export default defineConfig({
  /** use to runtime, you can use that in your code */
  define: {},
  /** Remove production environment console */
  // extraBabelPlugins: ['transform-remove-console'],
})
